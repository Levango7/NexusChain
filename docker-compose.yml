version: '3.8'

# NexusChain 开发环境编排
# 
# 包含三类服务：
#   1. 应用服务（nexus-gateway / nexus-signing-service / nexus-wallet-service / nexus-core / nexus-bridge）—— 需各模块 Dockerfile 就绪后构建
#   2. 基础设施（nacos / sentinel-dashboard / seata-server / zipkin）—— 服务发现 + 配置中心 + 熔断限流控制台 + 分布式事务 + 链路追踪
#   3. nexus-pgsql（真机联调数据库）—— core 以 local profile 原生运行时使用的容器化 Postgres（见 nexus-core/nexus-core/src/main/resources/application-local.properties）
#      注：dev 与 prod compose 统一使用 postgres:16-alpine（REQ-28 已对齐）
# 
# 开发环境仅拉起基础设施：
#   docker-compose up nacos sentinel-dashboard seata-server zipkin
# 真机联调（容器 PG + 原生 core，推荐走 scripts/dev-pg-up.ps1 / dev-pg-up.sh 自动拉起）：
#   docker-compose up -d nexus-pgsql
# 全量拉起（含应用）：
#   docker-compose up
# 
# 端口约定（设计文档 §4.2.2 / §4.6.1 配置一致性修复）：
#   Nacos    : 8848 (HTTP) / 9848 (gRPC, Nacos 2.x client 必需)
#   Sentinel : 8858 (Dashboard UI)
#   Seata    : 8091 (TC) / 7091 (Web 控制台)
#   Zipkin   : 9411 (API + UI)
#   Postgres : 55432 (localhost 映射；与 application-local.properties 的 DATA_SOURCE_URL 对齐)
#   gateway  : 8080 / signing : 8082 / wallet : 8083 / bridge : 8084 / core : 19585

services:
  nexus-gateway:
    build: ./nexus-gateway
    ports:
      - '127.0.0.1:8080:8080'
    environment:
      # REQ-33/P2: dev compose 应使用 dev profile，prod profile 留给 docker-compose.prod.yml
      - SPRING_PROFILES_ACTIVE=dev
    depends_on:
      nexus-core:
        condition: service_healthy
      nacos:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8080/actuator/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  nexus-core:
    build: ./nexus-core
    ports:
      - '127.0.0.1:19585:19585'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:19585/height']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # === 签名服务（涉及私钥的敏感操作，独立部署单元） ===
  # 端口 8082，承载 /api/v1/transfers/sign + /signing/capability 端点。
  # 依赖 Seata Server（TCC 分布式事务）+ Nacos（服务注册 + 配置）。
  # 设计文档 §4.5.1。
  nexus-signing-service:
    build: ./nexus-signing-service
    ports:
      - '127.0.0.1:8082:8082'
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - NEX_NACOS_SERVER=nexus-nacos:8848
      - NEX_SEATA_SERVER=nexus-seata-server:8091
      - NEX_ZIPKIN_ENDPOINT=http://nexus-zipkin:9411/api/v2/spans
      # MPC-P1-05: 向 mpc-engine 发起 gRPC 调用的 Bearer token，须与 mpc-engine 的 MPC_AUTH_TOKEN 一致
      # P0-8 加固：开发默认值内联，生产必须通过环境变量 MPC_AUTH_TOKEN 覆盖（与 mpc-engine 各节点保持一致）
      - NEX_MPC_ENGINE_AUTH_TOKEN=${MPC_AUTH_TOKEN:-dev-mpc-engine-token-change-in-prod}
      # P0-1 MPC 分散式部署：多端点路由（Task 239）
      # 逗号分隔的 host:port 列表，MpcEngineRouter 按 partyIndex 路由到对应节点
      # 单端点配置时向后兼容（行为等价于原 mpc.engine.host:port）
      - NEX_MPC_ENGINE_ENDPOINTS=mpc-engine-0:50051,mpc-engine-1:50051,mpc-engine-2:50051
      # mTLS 证书路径（3 节点共用 CA，客户端证书用 node-A 签发的 signing-service 证书）
      - NEX_MPC_ENGINE_TLS_TRUST_CERT=/etc/mpc/certs/ca/CA.pem
      - NEX_MPC_ENGINE_TLS_CLIENT_CERT=/etc/mpc/certs/node-A/cert.pem
      - NEX_MPC_ENGINE_TLS_CLIENT_KEY=/etc/mpc/certs/node-A/key.pem
    depends_on:
      nacos:
        condition: service_healthy
      seata-server:
        condition: service_healthy
      mpc-engine-0:
        condition: service_started
      mpc-engine-1:
        condition: service_started
      mpc-engine-2:
        condition: service_started
    # REQ-34/P2: signing-service 加入 nexus-net 网络以访问 mpc-engine
    networks:
      - default
      - nexus-net
    volumes:
      # 挂载 mpc-certs 供 mTLS 客户端证书
      - ./mpc-certs:/etc/mpc/certs:ro
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8082/actuator/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # === 钱包管理服务（不涉及私钥的地址工具 + 提现审批 + 托管 + 白名单） ===
  # 端口 8083，承载 /api/v1/wallet/** 端点。
  # 依赖 Seata Server（提现审批 TCC）+ Nacos（服务注册 + 配置）。
  # 设计文档 §4.5.1。
  nexus-wallet-service:
    build: ./nexus-wallet-service
    ports:
      - '127.0.0.1:8083:8083'
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - NEX_NACOS_SERVER=nexus-nacos:8848
      - NEX_SEATA_SERVER=nexus-seata-server:8091
      - NEX_ZIPKIN_ENDPOINT=http://nexus-zipkin:9411/api/v2/spans
    depends_on:
      nacos:
        condition: service_healthy
      seata-server:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8083/actuator/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  nexus-bridge:
    build: ./nexus-bridge
    ports:
      - '127.0.0.1:8084:8084'   # 修正：原 8082 → 8084（与 signing-service 8082 冲突，见 §4.5.1）
    environment:
      # REQ-33/P2: dev compose 应使用 dev profile，prod profile 留给 docker-compose.prod.yml
      - SPRING_PROFILES_ACTIVE=dev
    depends_on:
      nexus-core:
        condition: service_healthy
      nacos:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8084/api/v1/bridge/status']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # === 服务发现 + 配置中心 ===
  # Nacos 2.3.2 standalone 模式，嵌入式 derby 持久化（开发环境，不依赖外部 MySQL）。
  # 生产环境请使用 docker-compose.prod.yml 中的 Nacos 集群 + MySQL 持久化方案。
  # 版本对齐设计文档 §4.1：SCA 2023.0.1.0 内置 Nacos Client 2.3.2。
  nacos:
    image: nacos/nacos-server:v2.3.2
    container_name: nexus-nacos
    ports:
      - '8848:8848'    # Nacos HTTP API + 控制台
      - '9848:9848'    # Nacos 2.x gRPC 端口（客户端必需，client ≥ 2.0 通过 gRPC 注册）
    environment:
      - MODE=standalone
      - PREFER_HOST_MODE=hostname
      - JVM_XMS=256m
      - JVM_XMX=512m
      - JVM_XMN=128m
      # 嵌入式 derby（开发环境默认）；如需 MySQL 持久化，取消注释以下配置：
      # - SPRING_DATASOURCE_PLATFORM=mysql
      # - MYSQL_SERVICE_HOST=nacos-mysql
      # - MYSQL_SERVICE_PORT=3306
      # - MYSQL_SERVICE_DB_NAME=nacos_config
      # - MYSQL_SERVICE_USER=nacos
      # - MYSQL_SERVICE_PASSWORD=nacos
    volumes:
      - nacos-logs:/home/nacos/logs
      - ./nacos-config/init:/home/nacos/init.d   # 自定义初始化脚本（见 nacos-config/）
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8848/nacos/v1/console/health/readiness']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # === 熔断 / 限流控制台 ===
  # Sentinel Dashboard 1.8.8，用于查看实时监控数据与推送限流规则。
  # 规则持久化到 Nacos 由各微服务 spring.cloud.sentinel.datasource 配置（见 §4.3.3）。
  # 版本对齐设计文档 §4.1：SCA 2023.0.1.0 内置 Sentinel 1.8.8。
  sentinel-dashboard:
    image: bladex/sentinel-dashboard:1.8.8
    container_name: nexus-sentinel-dashboard
    ports:
      - '8858:8080'    # 对外 8858 → 容器内 Sentinel Dashboard 默认 8080
    environment:
      - JAVA_OPTS=-Dserver.port=8080 -Dcsp.sentinel.dashboard.server=localhost:8080 -Dproject.name=nexus-sentinel-dashboard
    volumes:
      - sentinel-logs:/root/logs
    depends_on:
      nacos:
        condition: service_healthy
    restart: unless-stopped

  # === 分布式事务协调器 ===
  # Seata Server 2.0.0，TC（Transaction Coordinator）角色。
  # 注册到 Nacos（复用 nexus-nacos），配置中心指向 Nacos（namespace=dev, group=SEATA_GROUP）。
  # 开发环境 STORE_MODE=file（生产环境建议 db 模式 + MySQL 持久化）。
  # 版本对齐设计文档 §4.2.1：与 SCA 2023.0.1.0 seata-spring-boot-starter 兼容。
  seata-server:
    image: seataio/seata-server:2.0.0
    container_name: nexus-seata-server
    ports:
      - '8091:8091'    # Seata Server TC 端口（client 通过此端口注册分支事务）
      - '7091:7091'    # Seata Server Web 控制台
    environment:
      - SEATA_PORT=8091
      - STORE_MODE=file
      # 注册到 Nacos（复用 nexus-nacos）
      - SEATA_REGISTRY_NAME=nacos
      - SEATA_REGISTRY_NACOS_SERVER_ADDR=nexus-nacos:8848
      - SEATA_REGISTRY_NACOS_NAMESPACE=dev
      - SEATA_REGISTRY_NACOS_GROUP=SEATA_GROUP
      - SEATA_REGISTRY_NACOS_CLUSTER_NAME=default
      # 配置中心指向 Nacos
      - SEATA_CONFIG_NAME=nacos
      - SEATA_CONFIG_NACOS_SERVER_ADDR=nexus-nacos:8848
      - SEATA_CONFIG_NACOS_NAMESPACE=dev
      - SEATA_CONFIG_NACOS_GROUP=SEATA_GROUP
    depends_on:
      nacos:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'bash -c "echo > /dev/tcp/localhost/8091"']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # === 链路追踪 ===
  # Zipkin Server 3.4，收集 Micrometer Tracing + Brave 上报的 span。
  # 开发环境 STORAGE_TYPE=mem（内存存储，重启丢失；生产环境建议 mysql 或 elasticsearch）。
  # 设计文档 §4.3.4：4 服务通过 zipkin-reporter-brave HTTP POST 上报至 /api/v2/spans。
  zipkin:
    image: openzipkin/zipkin:3.4
    container_name: nexus-zipkin
    ports:
      - '9411:9411'    # Zipkin API + UI
    environment:
      - STORAGE_TYPE=mem   # 开发：内存存储；生产：mysql 或 elasticsearch
      # 生产 MySQL 持久化：
      # - STORAGE_TYPE=mysql
      # - MYSQL_HOST=zipkin-mysql
      # - MYSQL_USER=zipkin
      # - MYSQL_PASS=zipkin
    healthcheck:
      test: ['CMD-SHELL', 'bash -c "echo > /dev/tcp/localhost/9411"']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  # === MPC 密码学引擎（Rust 独立进程，3 节点分散式部署） ===
  # 审计报告 §4.1 方案 A：GG18/GG20 密码学核心由 Rust tss-lib / multi-party-ecdsa 实现，
  # signing-service 通过容器网络 gRPC 调用。
  #
  # P0-1 Task 239：从单节点升级为 3 节点分散式部署（2-of-3 阈值签名）
  #   mpc-engine-0 → party_index=0, 端口 50051, 证书 node-A
  #   mpc-engine-1 → party_index=1, 端口 50052, 证书 node-B
  #   mpc-engine-2 → party_index=2, 端口 50053, 证书 node-C
  # 三节点共享 nexus-net 网络，通过服务名互连（mpc-engine-0:50051 等）
  # signing-service 通过 NEX_MPC_ENGINE_ENDPOINTS 多端点路由（MpcEngineRouter）
  #
  # 注：端口 50051/50052/50053 仅本机暴露用于调试（grpcurl）；
  #     容器间通信走 nexus-net 网络的 50051 端口（每容器内部均监听 50051）。
  mpc-engine-0:
    build:
      context: ./mpc-engine
      dockerfile: Dockerfile
    container_name: nexus-mpc-engine-0
    ports:
      - '127.0.0.1:50051:50051'    # gRPC，仅本机访问（容器间用服务名 mpc-engine-0:50051）
    environment:
      - RUST_LOG=info
      - MPC_ENGINE_HOST=0.0.0.0
      - MPC_ENGINE_PORT=50051
      # P0-1: party_index 通过环境变量注入（0/1/2 对应 3 副本 ordinal）
      - MPC_PARTY_INDEX=0
      - MPC_PARTY_ID=party-0
      # P0-1: peers 列表（排除本节点的其他参与方端点）
      - MPC_PEERS=party-1@mpc-engine-1:50051,party-2@mpc-engine-2:50051
      # MPC-P1-05 安全止血：会话落盘 AES-256-GCM 加密密钥
      # P0-8 加固：开发默认值内联，生产必须通过环境变量覆盖或使用 docker-compose.prod.yml
      - MPC_STORAGE_KEY=${MPC_STORAGE_KEY:-0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
      # MPC-P1-05: gRPC Bearer token 认证
      - MPC_AUTH_TOKEN=${MPC_AUTH_TOKEN:-dev-mpc-engine-token-change-in-prod}  # P0-8: dev default; override in prod
      - MPC_REQUIRE_AUTH=true
    volumes:
      # 每节点挂载独立证书（node-A/B/C）+ 共享 CA
      - ./mpc-certs/ca:/etc/mpc/tls/ca:ro
      - ./mpc-certs/node-A:/etc/mpc/tls/node:ro
      - mpc-engine-0-data:/app/data
    networks:
      - nexus-net
    restart: unless-stopped

  mpc-engine-1:
    build:
      context: ./mpc-engine
      dockerfile: Dockerfile
    container_name: nexus-mpc-engine-1
    ports:
      - '127.0.0.1:50052:50051'    # 宿主机 50052 → 容器内 50051
    environment:
      - RUST_LOG=info
      - MPC_ENGINE_HOST=0.0.0.0
      - MPC_ENGINE_PORT=50051
      - MPC_PARTY_INDEX=1
      - MPC_PARTY_ID=party-1
      - MPC_PEERS=party-0@mpc-engine-0:50051,party-2@mpc-engine-2:50051
      # P0-8 加固：开发默认值内联，生产必须通过环境变量覆盖或使用 docker-compose.prod.yml
      - MPC_STORAGE_KEY=${MPC_STORAGE_KEY:-0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
      - MPC_AUTH_TOKEN=${MPC_AUTH_TOKEN:-dev-mpc-engine-token-change-in-prod}  # P0-8: dev default; override in prod
      - MPC_REQUIRE_AUTH=true
    volumes:
      - ./mpc-certs/ca:/etc/mpc/tls/ca:ro
      - ./mpc-certs/node-B:/etc/mpc/tls/node:ro
      - mpc-engine-1-data:/app/data
    networks:
      - nexus-net
    restart: unless-stopped

  mpc-engine-2:
    build:
      context: ./mpc-engine
      dockerfile: Dockerfile
    container_name: nexus-mpc-engine-2
    ports:
      - '127.0.0.1:50053:50051'    # 宿主机 50053 → 容器内 50051
    environment:
      - RUST_LOG=info
      - MPC_ENGINE_HOST=0.0.0.0
      - MPC_ENGINE_PORT=50051
      - MPC_PARTY_INDEX=2
      - MPC_PARTY_ID=party-2
      - MPC_PEERS=party-0@mpc-engine-0:50051,party-1@mpc-engine-1:50051
      # P0-8 加固：开发默认值内联，生产必须通过环境变量覆盖或使用 docker-compose.prod.yml
      - MPC_STORAGE_KEY=${MPC_STORAGE_KEY:-0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
      - MPC_AUTH_TOKEN=${MPC_AUTH_TOKEN:-dev-mpc-engine-token-change-in-prod}  # P0-8: dev default; override in prod
      - MPC_REQUIRE_AUTH=true
    volumes:
      - ./mpc-certs/ca:/etc/mpc/tls/ca:ro
      - ./mpc-certs/node-C:/etc/mpc/tls/node:ro
      - mpc-engine-2-data:/app/data
    networks:
      - nexus-net
    restart: unless-stopped

  # === ZK Groth16 真实验证服务（Rust arkworks，BN254 配对）===
  # gRPC 50061 + HTTP 50062（Java 桥接 /v1/verify）。setup 持久化到命名卷
  # （可信设置仪式产出经 GROTH16_EXTERNAL_SETUP_DIR 挂载或 /v1/setup-external 导入）。
  zk-groth16-service:
    build:
      context: ./zk-groth16-service
      dockerfile: Dockerfile
    ports:
      - '127.0.0.1:50061:50061'    # gRPC
      - '127.0.0.1:50062:50062'    # HTTP JSON（Java HttpClient 桥接）
    environment:
      - ZK_SERVICE_PORT=50061
      - ZK_HTTP_PORT=50062
      - GROTH16_SETUP_DIR=/setup
    volumes:
      - zk-groth16-setup:/setup
    networks:
      - nexus-net
    restart: unless-stopped

  # === 真机联调数据库 ===
  # 用途：core 以 local profile 原生运行时（gradlew :nexus-core:nexus-core:run --args="--spring.profiles.active=local"）
  #       的容器化 Postgres。凭据与 application-local.properties 严格一致（nexus/nexus123/nexuschain）。
  #       端口 55432 仅绑定回环地址（与 nexus-core/README.md §2.3 安全建议一致）；
  #       幂等拉起见 scripts/dev-pg-up.ps1 / dev-pg-up.sh（已有健康 PG 时自动复用，不重复创建）。
  #       数据落在命名卷 pg-dev-data（与 prod compose 的 pgdata 隔离）；docker compose down 不删卷。
  nexus-pgsql:
    image: postgres:16-alpine
    container_name: nexus-pgsql
    ports:
      - '127.0.0.1:55432:5432'
    environment:
      - POSTGRES_DB=nexuschain
      - POSTGRES_USER=nexus
      - POSTGRES_PASSWORD=nexus123
    volumes:
      - pg-dev-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nexus -d nexuschain']
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
    restart: unless-stopped

volumes:
  nacos-logs:
  sentinel-logs:
  pg-dev-data:
  zk-groth16-setup:
  # P0-1 Task 239: MPC 3 节点独立数据卷（会话快照、WAL 持久化）
  mpc-engine-0-data:
  mpc-engine-1-data:
  mpc-engine-2-data:

networks:
  # MPC 引擎专用桥接网络。现有 Java 服务未显式声明 networks（使用默认 bridge），
  # signing-service 接入 mpc-engine 时需将二者置于同一网络（见上方注释）。
  nexus-net:
    driver: bridge
