# Build artifacts
build/
out/
target/
*.class
*.jar
*.war

# Gradle
.gradle/
gradle-app.setting
!gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
.factorypath

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Bundled JDK (use Gradle toolchain auto-provisioning instead)
jdk17/
jdk17.zip
*.zip

# Logs
*.log
logs/

# Local env
local.env
.env
.env.test
.env.test.local

# LevelDB data
leveldb/

# Runtime data
*.pid

# Test dump files (large binary fixtures removed in v1.9.2)
*.dmp

# AI agent working artifacts (not source)
.codeartsdoer/*
!.codeartsdoer/specs/
.codeartsdoer/specs/*
!.codeartsdoer/specs/audit-remediation-v2/
.codeartsdoer/specs/audit-remediation-v2/*
!.codeartsdoer/specs/audit-remediation-v2/optional-improvements.md

# Keystore / wallet files — NEVER commit private key material
nexus-sdk/java/Keystore/
*.keystore
/keystore/
*.jks
*.p12
*.pem
*.key

# 证书文件
*.crt
*.csr
*.der
*.cert
*.pfx

# SSH 私钥
id_rsa
id_ed25519
id_ecdsa

# 云凭证
credentials.json
*.kdbx

# 环境变量
.env.local
.env.*.local

# 临时文件
*.swp
*~
*.tmp
*.bak
*.rs.bk

# Runtime artifacts (mpc-engine 等长驻进程)
*.log
*.pid
nexus-local.env
local.env

# H2 embedded database files (local-dev fallback; see nexus-core build.gradle)
*.mv.db
*.trace.db

# AI agent working artifacts
.inscode/

# MPC mTLS 证书（含私钥，不入库）
mpc-certs/
mpc-engine/mpc-sessions/
nexus-core/nexus-core/data-*/
# start-mpc-cluster.sh 运行时生成（内嵌机器特定绝对证书路径 + 随机 storage_key）；
# TOML 模板（node*.toml）已入库，JSON 由脚本按需生成
mpc-engine/config/node*.json

# P1-D6 安全加固：本地敏感配置覆盖文件（含明文凭据，禁止入库）
# 开发者本地通过这些文件覆盖 ${VAR:-CHANGE_ME} 占位符，必须保留在本地
docker-compose.override.yml
docker-compose.override.*.yml
*.local.yml
*.local.properties
deploy/config/redis.local.conf
deploy/config/seata.local.properties
# 生产环境变量文件（含真实凭据，仅部署机本地存在）
.env.prod
.env.prod.local
.env.secrets

# k6 二进制工具（本地安装，不提交）
.tools/

# ==================== P0/密钥管理修复：保护生产配置 ====================
# Security: protect production configs
# 防止含明文凭据的 application.yml / application-prod.yml 被误提交。
# 注意：.gitignore 只对未跟踪的新文件生效。以下文件已被 git 跟踪，
# 若要让 .gitignore 规则生效，需手动执行：
#   git rm --cached <file>
# 当前 application.yml 已改为环境变量占位符（${NEXUS_*:}），不含明文密钥，
# 但仍建议各服务停止跟踪以防止未来硬编码敏感值回退。
**/src/main/resources/application-prod.yml
**/src/main/resources/application.yml
# 例外：开发/测试 profile 配置可入库（不含生产凭据）
!**/src/main/resources/application-dev.yml
!**/src/main/resources/application-test.yml

# Temp test debug artifacts
.test-batch/
.test-batch
# Gradle 命令行测试输出（临时重定向产物，不入库）
gw-test-output.txt
# 本地 AI 工具会话产物（memory/reports），不入库
.workbuddy/

# B6 修复（2026-08-31 交付前审计）：CI/调试日志与 JVM 崩溃转储不入库
# （审计发现根目录累积 86 个文件/13MB 的 dast-*/ci-logs-*/trivy-* 等）
ci-logs-*.txt
dast-*.txt
sec-dast-*.txt
sec-logs-*.txt
build-logs*.txt
trivy-*.log
hs_err_pid*.log
replay_pid*.log
