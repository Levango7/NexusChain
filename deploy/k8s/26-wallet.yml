# ============================================================================
# REQ-37 / T37: nexus-wallet-service Deployment + Service
# 端口 8083（与 application.yml server.port 对齐）
# 提现审批 TCC + 托管 + 白名单
# ============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-wallet-service
  namespace: nexus
  labels:
    app: nexus-wallet-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nexus-wallet-service
  template:
    metadata:
      labels:
        app: nexus-wallet-service
    spec:
      automountServiceAccountToken: false
      # REQ-40/P2: 跨可用区打散 Pod
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: nexus-wallet-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      # P2 加固：readOnlyRootFilesystem 配套 emptyDir 卷
      volumes:
        - name: tmp
          emptyDir: {}
        - name: spring-tmp
          emptyDir: {}
      containers:
        - name: wallet-service
          image: ghcr.io/levango7/nexuschain/nexus-wallet-service:2.1.0
          securityContext:
            allowPrivilegeEscalation: false
            # P2 加固：readOnlyRootFilesystem + emptyDir 挂载 /tmp
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: JAVA_TOOL_OPTIONS
              value: "-Djava.io.tmpdir=/tmp"
          envFrom:
            - configMapRef:
                name: nexus-config
            - secretRef:
                name: nexus-secrets
          # P2 加固：可写临时目录挂载为 emptyDir
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: spring-tmp
              mountPath: /app/tmp
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 40
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: nexus-wallet-service
  namespace: nexus
spec:
  type: ClusterIP
  selector:
    app: nexus-wallet-service
  ports:
    - port: 8083
      targetPort: 8083
