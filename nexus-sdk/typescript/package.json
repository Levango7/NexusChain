{
  "name": "@nexus/sdk",
  "version": "1.0.0",
  "description": "NexusChain TypeScript/JavaScript SDK (NEX)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist dist-test",
    "lint": "eslint src --ext .ts",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/sdk.test.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
