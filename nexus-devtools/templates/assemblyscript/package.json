{
  "name": "nexus-contract-template-as",
  "version": "0.1.0",
  "description": "NexusChain (NEX) 链上 AssemblyScript WASM 合约模板",
  "type": "module",
  "scripts": {
    "build": "asc assembly/index.ts --outFile build/contract.wasm --optimize --exportRuntime",
    "test": "node tests/"
  },
  "dependencies": {
    "as-serde": "^0.5.0"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  },
  "license": "MIT"
}
