plugins {
    id 'java'
}
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}
task printJdk {
    doLast {
        println javaToolchains.launcherFor(java.toolchain).get().metadata.installationPath
    }
}